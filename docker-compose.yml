version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: 'eliasablan'
    ports:
      - '5002:3000'
    volumes:
      - .:/app
      - /app/node_modules
