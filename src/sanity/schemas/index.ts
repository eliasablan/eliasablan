import home from './home'
import settings from './settings'
import projects from './projects'
import posts from './posts'
import tags from './tags'

export const schemaTypes = [home, settings, projects, posts, tags]
